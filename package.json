{
  "name": "worldmaster",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {},
  "dependencies": {
    "firebase-admin": "^4.0.4",
    "firebase-queue": "^1.6.0"
  },
  "scripts": {
    "start": "node --harmony-async-await queue",
    "deploy": "now -e NODE_ENV=production",
    "now-start": "node --harmony-async-await queue",
    "now-build": ""
  }
}
